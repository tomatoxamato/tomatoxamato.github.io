# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog.
# For more, see https://jekyllrb.com/docs/configuration/

title: tomatoxamato
email: your-email@example.com
description: >-
  A personal site hosted on GitHub Pages.
baseurl: "" # the subpath of your site, e.g. /blog (empty for a user site at the root)
url: "https://lsglc.com" # the base hostname & protocol for your site

# Social / identity used by the theme footer
github_username: tomatoxamato

# Build settings
theme: minima
plugins:
  - jekyll-feed

# Exclude compiled output and local junk from being processed
exclude:
  - Gemfile
  - Gemfile.lock
  - .git/
